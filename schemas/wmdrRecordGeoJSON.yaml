$schema: 'https://json-schema.org/draft/2020-12/schema'
$id: 'https://raw.githubusercontent.com/wmo-im/wmdr2/main/schemas/wmdrRecordGeoJSON.yaml'
title: WMDR station metadata record definition
description: WMDR station metadata record definition

properties:
  id:
    $ref: 'https://schemas.opengis.net/ogcapi/records/part1/1.0/openapi/schemas/recordGeoJSON.yaml#/properties/id'
    description: WIGOS station identifier
  conformsTo:
    type: array
    contains:
      const: http://wigos.wmo.int/spec/wmdr/2/conf/core
  type:
    $ref: 'https://schemas.opengis.net/ogcapi/records/part1/1.0/openapi/schemas/recordGeoJSON.yaml#/properties/type'
  geometry:
    $ref: 'https://schemas.opengis.net/ogcapi/features/part1/1.0/openapi/schemas/pointGeoJSON.yaml'
  time:
    $ref: 'https://raw.githubusercontent.com/wmo-im/wcmp2/refs/heads/main/schemas/wcmpRecordGeoJSON.yaml#/properties/time'
  temporalGeometry:
    type: object
    description: temporal geometry
    properties:
      type:
        type: string
        description: temporal geometry type
        const: MovingPoint
      coordinates:
        type: array
        description: coordinates
        minItems: 1
        items:
          $ref: 'https://schemas.opengis.net/ogcapi/features/part1/1.0/openapi/schemas/pointGeoJSON.yaml#/properties/coordinates'
      dates:
        $ref: 'temporal.yaml#/dates'
      methods:
        type: array
        description: methods
        items:
          type: string
          $comment: |-
            - https://codes.wmo.int/wmdr/GeopositioningMethod

  properties:
    type: object
    properties:
      title:
        type: string
        description: facility name (primary)
      additionalTitles:
        type: array
        minItems: 1
        items:
          type: string
          description: additional facility names
      additionalIds:
        type: array
        minItems: 1
        items:
          type: string
          description: additional WIGOS Station Identifiers (WSI)
      facilityType:
        type: string
        description: Station/platform type
        $comment: https://codes.wmo.int/wmdr/FacilityType
      facilitySets:
        type: array
        minItems: 1
        items:
          type: string
          description: Facility set identifier
      contacts:
        type: array
        minItems: 1
        items:
          allOf:
            - $ref: 'https://schemas.opengis.net/ogcapi/records/part1/1.0/openapi/schemas/contact.yaml'
              required:
                - organization
              additionalProperties: false
      externalIds:
        $ref: 'https://schemas.opengis.net/ogcapi/records/part1/1.0/openapi/schemas/recordCommonProperties.yaml#/properties/externalIds'
      created:
        $ref: 'https://schemas.opengis.net/ogcapi/records/part1/1.0/openapi/schemas/recordCommonProperties.yaml#/properties/created'
      updated:
        $ref: 'https://schemas.opengis.net/ogcapi/records/part1/1.0/openapi/schemas/recordCommonProperties.yaml#/properties/updated'
      keywords:
        $ref: 'https://schemas.opengis.net/ogcapi/records/part1/1.0/openapi/schemas/recordCommonProperties.yaml#/properties/keywords'
      themes:
        $ref: 'https://schemas.opengis.net/ogcapi/records/part1/1.0/openapi/schemas/recordCommonProperties.yaml#/properties/themes'
      temporalTerritories:
        type: array
        description: time series of territories
        minItems: 1
        items:
          type: object
          properties:
            territory:
              type: string
              description: Territory of origin of data
              $comment: https://codes.wmo.int/wmdr/TerritoryName
            date:
              $ref: 'temporal.yaml#/dates/items'
      temporalProgramAffiliations:
        type: array
        description: time series of program affiliations
        minItems: 1
        items:
          type: object
          properties:
            facilityId:
              type: string
              description: Programme specific facility identifier
            facilityTitle:
              type: string
              description: Programme specific facility name
            programAffiliation:
              type: string
              description: Programme/network affiliation
              $comment: https://codes.wmo.int/wmdr/ProgramAffiliation
            reportingStatus:
              type: string
              description: Station/Platform operating status
              $comment: https://codes.wmo.int/wmdr/ReportingStatus
            date:
              $ref: 'temporal.yaml#/dates/items'
      deployments:
          type: array
          description: facility deployments
          minItems: 1
          items:
              $ref: 'deployment.yaml'
      instruments:
          type: array
          description: facility instruments
          minItems: 1
          items:
              $ref: 'instrument.yaml'
    required:
      - contacts
      - created
      - title
      - deployments
      - instruments

  links:
    type: array
    minItems: 1
    items:
      $ref: 'https://raw.githubusercontent.com/wmo-im/wcmp2/refs/heads/main/schemas/link.yaml'

required:
  - id
  - conformsTo
  - type
  - geometry
  - properties
  - links
